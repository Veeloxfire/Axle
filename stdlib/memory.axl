#import #std("kernel32");

heap_handle : $dynamic = GetProcessHeap();

allocate := (bytes: u64) -> *void {
  return HeapAlloc(heap_handle, 0, bytes);
}

free := (ptr: *void) -> void {
  HeapFree(heap_handle, 0, ptr);
}