counter: u64 = 0;

fib :: (a: u64) -> u64 {
  counter = counter + 1;
  if(a == 1 | a == 0) {
    return 1;
  }
  else {
    return fib(a - 1) + fib(a - 2);
  }
}

main :: () -> u64 {
  return fib(5);
}

