#import "sandbox2.axl";

function main() -> u64 {
  u64 handle = GetStdHandle(-11);

  [ascii; 14] hello_world = "Hello World!\n";
  u32 res = 0;

  WriteConsoleA(handle, cast(*ascii, hello_world), 14, &res, nullptr);

  return cast(u64, res);
}